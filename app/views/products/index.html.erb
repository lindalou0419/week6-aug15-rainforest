<section class="main-page">
  <h1>List of all products</h1>

  <% @products.each do |pro| %>
    <ul class="product-wrapper">
      <li><%= link_to(pro.name, product_path(pro)) %></li>
      <li><%= pro.description %></li>
      <li><%= pro.price_in_cents %></li>
    </ul>
  <% end %>
</section>